<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee 
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Wicket Publisher</display-name>
    <description>Módulo Wicket para publicação de dados do placar em tempo real</description>

    <!-- Context Parameters -->
    <context-param>
        <param-name>configuration</param-name>
        <param-value>development</param-value>
    </context-param>

    <!-- CDI Configuration -->
    <context-param>
        <param-name>jakarta.enterprise.inject.scan.implicit</param-name>
        <param-value>true</param-value>
    </context-param>

    <!-- Wicket Application Filter -->
    <filter>
        <filter-name>wicket.placar-realtime</filter-name>
        <filter-class>org.apache.wicket.protocol.http.WicketFilter</filter-class>
        <init-param>
            <param-name>applicationClassName</param-name>
            <param-value>br.com.solides.placar.wicket.WicketPublisherApplication</param-value>
        </init-param>
    </filter>

    <filter-mapping>
        <filter-name>wicket.placar-realtime</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Security Constraints -->
    <security-constraint>
        <display-name>Secure Application</display-name>
        <web-resource-collection>
            <web-resource-name>Protected Area</web-resource-name>
            <url-pattern>/admin/*</url-pattern>
            <http-method>GET</http-method>
            <http-method>POST</http-method>
        </web-resource-collection>
        <!-- Remove auth-constraint for now to keep it simple -->
    </security-constraint>

    <!-- Error Pages -->
    <error-page>
        <error-code>404</error-code>
        <location>/error/404.html</location>
    </error-page>

    <error-page>
        <error-code>500</error-code>
        <location>/error/500.html</location>
    </error-page>

    <!-- Session Configuration -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
            <secure>false</secure>
        </cookie-config>
        <tracking-mode>COOKIE</tracking-mode>
    </session-config>

    <!-- Welcome Files -->
    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- MIME Types for Static Resources -->
    <mime-mapping>
        <extension>css</extension>
        <mime-type>text/css</mime-type>
    </mime-mapping>
    
    <mime-mapping>
        <extension>js</extension>
        <mime-type>application/javascript</mime-type>
    </mime-mapping>
    
    <mime-mapping>
        <extension>json</extension>
        <mime-type>application/json</mime-type>
    </mime-mapping>

    <!-- Resource References -->
    <resource-ref>
        <description>Primary DataSource</description>
        <res-ref-name>jdbc/PlacarDS</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>

</web-app>